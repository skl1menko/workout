# Workout App - Рефакторинг структуры проекта

## ✅ Что было сделано

Проект был полностью реорганизован для улучшения читаемости, масштабируемости и поддерживаемости кода.

## 📦 Новая структура

### Создана папка `src/` с модульной архитектурой:

```
src/
├── components/          # Переиспользуемые компоненты
│   ├── ui/             # Базовые UI компоненты
│   │   ├── Button.tsx
│   │   ├── HealthCard.tsx
│   │   ├── DateNavigator.tsx
│   │   ├── ErrorMessage.tsx
│   │   └── LoadingIndicator.tsx
│   └── health/         # Специфичные компоненты здоровья
│       ├── PermissionRequest.tsx
│       └── HealthMetricsGrid.tsx
│
├── screens/            # Экраны приложения
│   ├── HomeScreen.tsx
│   └── HealthScreen.tsx
│
├── services/           # Бизнес-логика и API
│   └── health.service.ts
│
├── hooks/              # Кастомные React хуки
│   └── use-health-data.ts
│
├── types/              # TypeScript типы
│   └── health.types.ts
│
├── styles/             # Дизайн-система
│   ├── colors.ts
│   ├── typography.ts
│   └── spacing.ts
│
└── utils/              # Вспомогательные функции
    ├── date.utils.ts
    └── health.utils.ts
```

## 🎯 Ключевые улучшения

### 1. **Разделение ответственности**
- **Components**: Только UI, никакой бизнес-логики
- **Services**: Взаимодействие с HealthKit API
- **Hooks**: Переиспользуемая логика состояния
- **Utils**: Чистые функции-помощники
- **Screens**: Композиция компонентов

### 2. **Переиспользуемые UI компоненты**
Созданы универсальные компоненты:
- `Button` - кнопка с вариантами стилей
- `HealthCard` - карточка для метрик
- `DateNavigator` - навигация по датам
- `ErrorMessage` - отображение ошибок
- `LoadingIndicator` - индикатор загрузки

### 3. **Сервисный слой**
Вся работа с HealthKit вынесена в `health.service.ts`:
- Проверка доступности HealthKit
- Управление разрешениями
- Получение данных (шаги, калории, пульс, сон)
- Централизованная обработка ошибок

### 4. **Дизайн-система**
Константы стилей собраны в отдельные файлы:
```typescript
import { ColorScheme, Spacing, FontSizes, BorderRadius } from '@/src/styles';
```
- Единообразие дизайна
- Легко менять темы
- Консистентность UI

### 5. **Типизация TypeScript**
Все типы централизованы в `src/types/`:
```typescript
export interface HealthMetrics {
  steps: number;
  calories: number;
  heartRate: HealthValue[];
  sleep: HealthValue[];
}
```

### 6. **Утилиты**
Переиспользуемые функции:
- Работа с датами (`date.utils.ts`)
- Расчёты метрик (`health.utils.ts`)

### 7. **Централизованные экспорты**
Каждая папка имеет `index.ts`:
```typescript
// Простой импорт
import { Button, HealthCard } from '@/src/components';
// Вместо
import { Button } from '@/src/components/ui/Button';
import { HealthCard } from '@/src/components/ui/HealthCard';
```

## 🔄 Что изменилось в существующих файлах

### `app/(tabs)/index.tsx`
```typescript
// Было: Весь код компонента
// Стало: 
import { HomeScreen } from '@/src/screens';
export default HomeScreen;
```

### `app/(tabs)/health.tsx`
```typescript
// Было: 200+ строк кода
// Стало:
import { HealthScreen } from '@/src/screens';
export default HealthScreen;
```

### `hooks/use-health-data.ts` → `src/hooks/use-health-data.ts`
- Переписан с использованием нового сервисного слоя
- Использует async/await вместо коллбэков
- Улучшена обработка ошибок

## 📚 Документация

Добавлен `src/README.md` с:
- Описанием структуры
- Принципами организации
- Примерами использования
- Соглашениями о коде

## 🚀 Преимущества новой структуры

✅ **Масштабируемость** - легко добавлять новые фичи  
✅ **Читаемость** - понятная иерархия файлов  
✅ **Переиспользование** - компоненты изолированы  
✅ **Тестируемость** - логика отделена от UI  
✅ **Поддержка** - легко найти нужный код  
✅ **Типобезопасность** - централизованные типы  
✅ **Консистентность** - единая дизайн-система  

## 📝 Следующие шаги

Для дальнейшего развития проекта:

1. **Добавить тесты**:
   - Unit тесты для утилит
   - Тесты компонентов
   - Тесты сервисов

2. **Расширить функционал**:
   - Новые метрики здоровья
   - Графики и визуализации
   - История данных

3. **Улучшить UX**:
   - Темная тема
   - Анимации
   - Офлайн режим

## 💡 Как использовать

При добавлении нового функционала:

1. **Новый экран** → `src/screens/`
2. **Новый UI компонент** → `src/components/ui/`
3. **Новая фича** → создать папку в `src/components/`
4. **Новый сервис** → `src/services/`
5. **Новый хук** → `src/hooks/`
6. **Новые типы** → `src/types/`

Не забывайте экспортировать через `index.ts`!

## 🔍 Старые файлы

Старые файлы остались, но больше не используются:
- `hooks/use-health-data.ts` (заменён на `src/hooks/use-health-data.ts`)
- `constants/theme.ts` (заменён на `src/styles/`)

Их можно безопасно удалить после проверки работоспособности.
