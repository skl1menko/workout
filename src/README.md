# Структура проекта Workout App

## 📁 Организация кода

Проект организован по принципу **feature-based architecture** с разделением по функциональности:

```
src/
├── components/          # Переиспользуемые компоненты
│   ├── ui/             # Базовые UI компоненты
│   │   ├── Button.tsx
│   │   ├── HealthCard.tsx
│   │   ├── DateNavigator.tsx
│   │   ├── ErrorMessage.tsx
│   │   ├── LoadingIndicator.tsx
│   │   └── index.ts
│   ├── health/         # Компоненты для функционала здоровья
│   │   ├── PermissionRequest.tsx
│   │   ├── HealthMetricsGrid.tsx
│   │   └── index.ts
│   └── index.ts
│
├── screens/            # Экраны приложения
│   ├── HomeScreen.tsx
│   ├── HealthScreen.tsx
│   └── index.ts
│
├── services/           # Бизнес-логика и API
│   ├── health.service.ts
│   └── index.ts
│
├── hooks/              # Пользовательские React хуки
│   ├── use-health-data.ts
│   └── index.ts
│
├── types/              # TypeScript типы и интерфейсы
│   ├── health.types.ts
│   └── index.ts
│
├── styles/             # Стили и дизайн-система
│   ├── colors.ts       # Цветовая палитра
│   ├── typography.ts   # Шрифты и размеры
│   ├── spacing.ts      # Отступы и радиусы
│   └── index.ts
│
├── utils/              # Вспомогательные функции
│   ├── date.utils.ts   # Работа с датами
│   ├── health.utils.ts # Расчёты метрик здоровья
│   └── index.ts
│
└── index.ts            # Центральный экспорт всех модулей
```

## 🎯 Принципы организации

### 1. **Модульность**
Каждая папка содержит `index.ts` для централизованного экспорта, что упрощает импорты:
```typescript
// Вместо
import { Button } from '@/src/components/ui/Button';
// Можно писать
import { Button } from '@/src/components';
```

### 2. **Разделение ответственности**

- **Components** - только UI, без бизнес-логики
- **Services** - взаимодействие с внешними API (HealthKit)
- **Hooks** - переиспользуемая логика состояния
- **Utils** - чистые функции без побочных эффектов
- **Screens** - композиция компонентов для конкретных экранов

### 3. **Типизация**
Все типы собраны в `src/types` для переиспользования и централизованного управления.

### 4. **Дизайн-система**
Стили вынесены в отдельные файлы:
- `colors.ts` - цветовая схема
- `typography.ts` - шрифты и размеры текста
- `spacing.ts` - отступы и радиусы скругления

## 🔧 Как использовать

### Импорты
Используйте alias `@/src` для импорта из папки src:
```typescript
import { HealthScreen } from '@/src/screens';
import { useHealthData } from '@/src/hooks';
import { ColorScheme, Spacing } from '@/src/styles';
```

### Добавление нового функционала

1. **Новый экран**: создайте в `src/screens/`
2. **Новый компонент**: 
   - UI компонент → `src/components/ui/`
   - Специфичный компонент → создайте новую папку в `src/components/`
3. **Новый сервис**: добавьте в `src/services/`
4. **Новый хук**: добавьте в `src/hooks/`
5. **Новые типы**: добавьте в `src/types/`

Не забудьте экспортировать через соответствующий `index.ts`!

## 📝 Соглашения о коде

- **Файлы**: PascalCase для компонентов (`Button.tsx`), kebab-case для утилит (`date.utils.ts`)
- **Компоненты**: Functional Components с TypeScript
- **Стили**: StyleSheet API из React Native
- **Именование**: Descriptive names, избегайте сокращений
- **Комментарии**: JSDoc для публичных функций и компонентов

## 🚀 Преимущества этой структуры

✅ **Масштабируемость** - легко добавлять новые фичи
✅ **Читаемость** - понятная иерархия файлов
✅ **Переиспользование** - компоненты и утилиты в отдельных модулях
✅ **Тестируемость** - изолированная бизнес-логика
✅ **Поддержка** - легко найти и изменить код
✅ **Типобезопасность** - централизованные типы TypeScript
